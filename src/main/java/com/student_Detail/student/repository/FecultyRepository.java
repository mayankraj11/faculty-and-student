package com.student_Detail.student.repository;

import com.student_Detail.student.model.Feculty;
import org.springframework.data.jpa.repository.JpaRepository;

public interface FecultyRepository extends JpaRepository <Feculty,Long>{
}
