package com.student_Detail.student.repository;

import com.student_Detail.student.model.Faculty;
import org.springframework.data.jpa.repository.JpaRepository;

public interface FecultyRepository extends JpaRepository <Faculty,Long>{
}
